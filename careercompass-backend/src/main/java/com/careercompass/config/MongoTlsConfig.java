package com.careercompass.config;

/**
 * @deprecated Replaced by {@link MongoConfig} which programmatically builds
 *             a MongoClient with a TLS-1.2-only SSLContext. This class is kept
 *             as a
 *             placeholder to avoid breaking any IDE run configs that may
 *             reference it.
 *             It has no active Spring beans.
 */
public class MongoTlsConfig {
}
